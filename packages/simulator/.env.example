# Server-side only — the service_role key bypasses RLS, never ship this to
# a browser bundle. Copy to .env.local and fill in from the Supabase
# dashboard (Project Settings → API).
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
